@model IEnumerable<AplicacaoMedicina.Models.UsuarioLaboratorio>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Laboratorio.Nome_Labo)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Usuario_UsuLab)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Senha_UsuLab)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Laboratorio.Nome_Labo)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Usuario_UsuLab)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Senha_UsuLab)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Id_UsuLab }) |
            @Html.ActionLink("Details", "Details", new { id=item.Id_UsuLab }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Id_UsuLab })
        </td>
    </tr>
}

</table>
