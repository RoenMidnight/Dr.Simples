@model IEnumerable<AplicacaoMedicina.Models.UsuarioPaciente>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Pacientes.Nome_Paci)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Usuario_UsuPaci)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Senha_UsuPaci)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Pacientes.Nome_Paci)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Usuario_UsuPaci)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Senha_UsuPaci)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Id_UsuPaci }) |
            @Html.ActionLink("Details", "Details", new { id=item.Id_UsuPaci }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Id_UsuPaci })
        </td>
    </tr>
}

</table>
