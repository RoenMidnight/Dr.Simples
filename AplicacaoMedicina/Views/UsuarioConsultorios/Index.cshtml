@model IEnumerable<AplicacaoMedicina.Models.UsuarioConsultorio>

@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
</head>
<body>
    <p>
        @Html.ActionLink("Create New", "Create")
    </p>
    <table class="table">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Consultorio.Nome_Consu)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Usuario_UsuCons)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Senha_UsuCons)
            </th>
            <th></th>
        </tr>
    
    @foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Consultorio.Nome_Consu)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Usuario_UsuCons)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Senha_UsuCons)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id=item.Id_UsuCons }) |
                @Html.ActionLink("Details", "Details", new { id=item.Id_UsuCons }) |
                @Html.ActionLink("Delete", "Delete", new { id=item.Id_UsuCons })
            </td>
        </tr>
    }
    
    </table>
</body>
</html>
